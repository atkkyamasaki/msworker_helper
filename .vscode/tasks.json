{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "node-sass --output-style compressed",
            "args": [
                "-r",
                "sass",
                "-o",
                "css"
            ],
            "group": "build",
            "problemMatcher": [
                "$node-sass"
            ]
        }
    ]
}
